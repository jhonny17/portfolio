name: Check linter
description: Checks the code meets the linter standards

inputs:
  github-token:
    description: 'GitHub token'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Install dependencies
      uses: ./.github/actions/install-dependencies
      with:
        github-token: ${{ inputs.github-token }}

    - name: Check linter
      shell: bash
      run: pnpm exec eslint
